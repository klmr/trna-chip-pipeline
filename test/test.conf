[project]
    name=test
    data_dir=data
    species=MM9

[map-reads]
    reference=${project:data_dir}/${project:species}

[trna-call-peaks]
    annotation=${project:data_dir}/tRNA.dat

[data]
    do1234=do1234_metadata.fq.gz
